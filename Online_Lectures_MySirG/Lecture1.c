// Write a function to count vowels in a given string
#include<stdio.h>
//int main()
/*int countVowel(char s[])
{
    int i,count=0;
    for(i=0; s[i]; i++)
    {
        if(s[i]=='a' || s[i]=='e' || s[i]=='i' || s[i]=='o' || s[i]=='u' || s[i]=='A' || s[i]=='E' || s[i]=='I' || s[i]=='O' || s[i]=='U')
        count++;
    }
    return count;
}
main()
{
    printf("%d", countVowel("MySirG Education Services" ));
}*/


// Writea function to check whether a given string is alphanumeric or not
bool isAlphaNumeric(char s[])
{
    int i, alpha=false, digit=false;
    for(i=0; s[i]; i++)
    {
        if(s[i]>='0' && s[i]<='9')
          digit=true;
        if()
    }
}