#include<stdio.h>
int main()
{
    int a, b;
    
    printf("Enter the 1 no: " );
    scanf("%d", &a);
    printf("Enter the 2 no: ");
    scanf("%d", &b);
    printf("Addition of two numbers: ");
    printf("%d + %d = %d ", a, b, a+b);
    return 0;   
}
